# types.h

## Description
Core primitive type definitions for exact bit-width unsigned and signed integers using the standard `<stdint.h>` equivalents.

## Constants/Macros
- `u8`, `u16`, `u32`, `u64`
- `s8`, `s16`, `s32`, `s64`
